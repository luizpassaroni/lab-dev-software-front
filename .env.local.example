# Envs server-only do BFF — lidas apenas nos route handlers do Next (/api/*).
# Nunca usar NEXT_PUBLIC_ aqui: o browser nao conhece a URL do back nem a chave interna.
API_INTERNAL_URL=
INTERNAL_API_KEY=
